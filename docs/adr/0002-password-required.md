# Password is required; Passkeys are extra

Every User has an Email and a Password so they can sign in on a new device. Passkeys are optional extras and may be many; they do not replace Password. Passkey-only Users (allowed in the current schema and `docs/backend-design.md`) are out of the domain because a single-device Passkey cannot reopen the User elsewhere.
